# RSR-SCANNER
RSR Scanner is a lightweight web-based QR code scanner that uses camera access to detect and decode QR codes in real time. It supports links and plain text output with a clean UI.
A fast, lightweight, and responsive QR Code Scanner web application built using HTML, CSS, and JavaScript.
